import { NstbalanceinputFilterPipe } from './nstbalanceinput-filter.pipe';

describe('NstbalanceinputPipe', () => {
  it('create an instance', () => {
    const pipe = new NstbalanceinputFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
